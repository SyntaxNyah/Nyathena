# fromsoft.txt — word list for the /fromsoftware punishment.
# One word per line. Lines starting with # are comments.
# Every occurrence of a listed word as a *substring* of any token is replaced
# with asterisks (one per letter), so short entries like "ho" will censor
# inside larger words — "should" → "s**uld", "how" → "**w". That's the joke.
#
# Example entries (replace or extend with your own):
bum
ass
damn
hell
crap
bastard
